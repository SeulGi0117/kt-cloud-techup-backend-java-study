package study.seulgi.java.ch01.basics.java;

public class OperatorMain {
	public static void main(String[] args) {
		// 비교연산자:> < >= <= == !=
		// 논리 연산자: &&(AND) ||(OR) !(NOT)

		// 우선순위 -> 논리 연상을 하는 경우가 보통 if 문에서 많이 사용
		// lase evaluation(Short-circuit evaluation)
		// 게으른 연산. 앞만보고 뒤는 안봐서 성능적인 측면에서 이점이 있음
		// &&(AND) : 앞이 false면 뒤는 안봄
		// ||(OR) : 앞이 ture면 뒤는 안봄

	}
}
